# ── Core ──────────────────────────────────────────────────────────────────────
Django==4.2.17
djangorestframework==3.15.2
django-cors-headers==4.3.1
Pillow==10.4.0

# ── Configuration & Environment ───────────────────────────────────────────────
python-decouple==3.8

# ── Filtering & Search ────────────────────────────────────────────────────────
django-filter==24.3

# ── API Documentation ─────────────────────────────────────────────────────────
drf-spectacular==0.27.2

# ── Production Server ─────────────────────────────────────────────────────────
gunicorn==22.0.0
whitenoise==6.7.0

# ── Production Database ───────────────────────────────────────────────────────
psycopg2-binary==2.9.9
dj-database-url==2.2.0

# ── File Storage (Supabase / S3-compatible) ───────────────────────────────────
django-storages==1.14.4
boto3==1.34.162

# ── Authentication — JWT ──────────────────────────────────────────────────────
djangorestframework-simplejwt==5.3.1

# ── Authentication — Google OAuth (via allauth) ───────────────
django-allauth==64.2.1
google-auth==2.29.0         # verifies Google ID tokens with audience validation

# ── Admin — Brute Force Protection ───────────────────────────────────────────
django-axes==6.5.1

# ── Admin — Two-Factor Authentication ────────────────────────────────────────
django-two-factor-auth==1.15.5
qrcode==7.4.2

# ── Password Hashing — Argon2 ────────────────────────────────────────────────
argon2-cffi==23.1.0

# ── Rate Limiting ─────────────────────────────────────────────────────────────
django-ratelimit==4.1.0

# ── Transitive deps that need explicit pinning ────────────────────────────────
cryptography==47.0.0        # required by allauth Google OIDC / JWT verification
phonenumbers==9.0.32        # required by django-two-factor-auth phonenumber plugin

# ── Security Auditing ─────────────────────────────────────────────────────────
pip-audit==2.7.3

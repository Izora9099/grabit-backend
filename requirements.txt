# ── Core ──────────────────────────────────────────────────────────────────────
Django>=4.2,<5.0
djangorestframework>=3.15
django-cors-headers>=4.3
Pillow>=10.0

# ── Configuration & Environment ───────────────────────────────────────────────
python-decouple>=3.8          # reads .env / environment variables cleanly

# ── Filtering & Search ────────────────────────────────────────────────────────
django-filter>=24.0           # DjangoFilterBackend for DRF querysets

# ── API Documentation ─────────────────────────────────────────────────────────
drf-spectacular>=0.27         # auto-generates OpenAPI 3 schema + Swagger UI

# ── Production Server ─────────────────────────────────────────────────────────
gunicorn>=22.0                # WSGI server for production deployment
whitenoise>=6.7               # serves compressed static files without a CDN

# ── Production Database ───────────────────────────────────────────────────────
psycopg2-binary>=2.9          # PostgreSQL adapter (pre-built binary, no pg_config needed)
dj-database-url>=2.0,<3.0     # parse DATABASE_URL env var into Django DATABASES dict (3.x requires Python 3.9+)

# ── File Storage (Supabase / S3-compatible) ───────────────────────────────────
django-storages>=1.14         # pluggable storage backends for Django
boto3>=1.34                   # AWS SDK used by django-storages S3 backend
